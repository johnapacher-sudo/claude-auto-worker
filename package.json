{
  "name": "claude-auto-worker",
  "version": "0.1.0",
  "description": "Run Claude Code as an unattended task worker — auto-execute task queues and generate reports",
  "main": "dist/index.js",
  "bin": {
    "auto-worker": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": ["claude", "claude-code", "automation", "task-runner", "worker"],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
